# 🚖 Taxi Fleet Manager

**Taxi Fleet Manager** — это мобильное приложение на Flutter для управления таксопарком. Приложение позволяет администраторам отслеживать состояние автомобилей, управлять списком водителей и вести учет транспортных средств в режиме реального времени.

## ✨ Основные возможности

* **📋 Список автомобилей**: Просмотр полного списка машин в автопарке с цветовой индикацией.
* **🔍 Детальная информация**: Просмотр карточки автомобиля с данными о водителе, гос. номере и текущем статусе.
* **🚦 Управление статусом**: Быстрое переключение статуса автомобиля («Свободен» / «Занят»).
* **➕ Добавление автомобилей**: Удобная форма для регистрации новых машин с выбором цвета кузова.
* **✏️ Редактирование**: Изменение данных о машине и водителе.
* **🗑️ Удаление**: Возможность списать автомобиль из автопарка.
* **🔄 Синхронизация**: Данные хранятся в облаке (Firebase) и обновляются через REST API.

## 🛠 Технологический стек

* **Фреймворк**: [Flutter](https://flutter.dev/) (Dart)
* **Управление состоянием**: [Riverpod](https://riverpod.dev/) (`StateNotifierProvider`, `FutureProvider`)
* **Бэкенд**: Firebase Realtime Database (через REST API)
* **Сеть**: пакет `http` для выполнения запросов (GET, POST, PATCH, DELETE)
* **Архитектура**: MVVM-style (Model - View - ViewModel/Notifier - Repository)

## 📂 Структура проекта

Проект организован следующим образом для удобства масштабирования:

```text
lib/
├── controllers/      # Логика управления состоянием (CarNotifier)
├── data/             # Репозитории для работы с данными
├── models/           # Модели данных (CarModel)
├── providers/        # Объявления Riverpod провайдеров
├── screens/          # Экраны приложения (Home, Add, Edit, Details)
├── services/         # Сервисы API (FirebaseService)
├── utils/            # Утилиты (константы, цвета)
└── widgets/          # Переиспользуемые виджеты (CarCard, ColorPicker)



